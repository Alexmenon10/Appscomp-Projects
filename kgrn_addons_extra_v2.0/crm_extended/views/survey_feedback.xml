<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <record id="survey_survey_view_form" model="ir.ui.view">
            <field name="name">survey.survey.view.form.inherit.website_slides</field>
            <field name="model">survey.survey</field>
            <field name="inherit_id" ref="survey.survey_form"/>
            <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Service Engagement">
                    <field name="service_engagement_ids">
                        <tree editable="bottom" create="0" delete="0">
                            <field name="service_engagement_id"/>
                            <field name="partner_id"/>
                            <field name="partner_mail"/>
                        </tree>
                    </field>
                </page>
            </xpath>
            </field>
        </record>

        <record model="ir.ui.view" id="survey_invite_view_form">
            <field name="name">survey.invite.view.form.inherit.service</field>
            <field name="model">survey.invite</field>
            <field name="inherit_id" ref="survey.survey_invite_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_ids']" position="attributes">
                    <attribute name="readonly">0</attribute>
                </xpath>
                <xpath expr="//field[@name='emails']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>
            </field>
        </record>
    </data>
</odoo>